import {StyleSheet} from 'react-native';

const styles = theme =>
  StyleSheet.create({
    input: {
      borderColor: 'gray',
      width: '95%',
      borderWidth: 1,
      borderRadius: 12,
      padding: 10,
      margin: 5,
    },
  });

export default styles;
